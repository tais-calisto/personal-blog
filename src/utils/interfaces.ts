export interface Categories {
  name: string;
}

export interface Posts {
  id: string;
  created_at: string;
  title: string;
  resume: string;
  content: string;
  category_id: string;
  image: string;
}
